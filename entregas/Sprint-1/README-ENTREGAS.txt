Entregas - Challenge Aguia Branca — Sprint 1
=============================================

Pasta: entregas/Sprint-1/

1. aguiabranca-debug.apk
   APK Android assinado em modo debug (protótipo do primeiro semestre).
   Instalar diretamente no dispositivo (habilitar "fontes desconhecidas").
   Tamanho: ~67 MB.

2. codigo-fonte-completo.zip
   Codigo-fonte do projeto Android na Sprint 1.
   Inclui: app/ (sources + manifest + resources), gradle/, gradlew*, build.gradle.kts,
   settings.gradle.kts, gradle.properties, README.md.
   Exclui: build outputs, caches do Gradle/IDE, .git e local.properties.

3. documentacao/documentacao-tecnica.pdf
   Documentacao tecnica da Sprint 1 em PDF (tecnologias, arquitetura, fluxo, perfis).
   Formato recomendado para a banca (renderizacao fixa).

4. video/
   Pasta reservada ao video da Sprint 1 (vazia nesta copia).

Contas demo (login do prototipo)
================================
- Operador  : operador@innovatecorp.com / oper123
- Gestor    : gestor@innovatecorp.com   / gest123
- Lideranca : lideranca@innovatecorp.com / lider123

A entrega da Sprint 2 (API + app integrado) fica em entregas/Sprint-2/.
